Prompt para Aider
Actúa como un Desarrollador Senior de Python y experto en Streamlit.

Objetivo: Modificar la vista de cálculo mensual para que, cuando un periodo de planilla esté en estado "CERRADA", se oculte la interfaz interactiva de Tesorería (botones de descarga y advertencias de cálculo) y se redirija al usuario al módulo de Reportería.

Archivos a modificar:

presentation/views/calculo_mensual.py

Análisis de Seguridad:

Debemos verificar el estado del periodo consultando la tabla PlanillaMensual antes de renderizar la sección de tesorería.

La variable periodo_key ya está disponible en el scope del archivo.

No debemos afectar el "Panel de Auditoría Tributaria", el cual debe seguir siendo visible para consultas rápidas.

Instrucciones estrictas de SEARCH/REPLACE:

En presentation/views/calculo_mensual.py, localiza la función _render_seccion_tesoreria y aplica los siguientes cambios:

Python
<<<<
    if not df_plan_glob.empty or not df_loc_glob.empty:
        st.markdown("---")
        st.subheader("🏦 Gestión de Tesorería")

        try:
            _db_chk = SessionLocal()
            _n_loc_glob = _db_chk.query(Trabajador).filter_by(empresa_id=empresa_id, situacion='ACTIVO', tipo_contrato='LOCADOR').count()
            _db_chk.close()
        except:
            _n_loc_glob = 0
        
        if _n_loc_glob > 0 and df_loc_glob.empty:
            st.warning("⚠️ Locadores detectados. Calcule Honorarios en la pestaña '🧾 2' para un reporte completo.")
        
        try:
            buf_teso_f = generar_pdf_tesoreria(
                df_planilla=df_plan_glob if not df_plan_glob.empty else None,
                df_loc=df_loc_glob if not df_loc_glob.empty else None,
                empresa_nombre=empresa_nombre,
                periodo_key=periodo_key,
                auditoria_data=aud_glob,
                empresa_ruc=st.session_state.get('empresa_activa_ruc', ''),
            )
            st.download_button(
                "🏦 Descargar Reporte de Tesorería (PDF)",
                data=buf_teso_f,
                file_name=f"TESORERIA_{periodo_key}.pdf",
                mime="application/pdf",
                use_container_width=True,
                type="primary",
                key="btn_teso_global_v_final"
            )
        except Exception: pass
====
    if not df_plan_glob.empty or not df_loc_glob.empty:
        st.markdown("---")
        st.subheader("🏦 Gestión de Tesorería")

        # Verificar si el periodo está cerrado
        es_cerrada = False
        try:
            db_chk_c = SessionLocal()
            plan_chk_c = db_chk_c.query(PlanillaMensual).filter_by(
                empresa_id=empresa_id, periodo_key=periodo_key
            ).first()
            if plan_chk_c and getattr(plan_chk_c, 'estado', 'ABIERTA') == 'CERRADA':
                es_cerrada = True
            db_chk_c.close()
        except Exception:
            pass

        if es_cerrada:
            st.info("ℹ️ **Periodo Cerrado:** Para obtener el reporte oficial de tesorería y archivos bancarios, debe ir al módulo **Reportería**, pestaña **🏦 Reporte Tesorería**.")
        else:
            try:
                _db_chk = SessionLocal()
                _n_loc_glob = _db_chk.query(Trabajador).filter_by(empresa_id=empresa_id, situacion='ACTIVO', tipo_contrato='LOCADOR').count()
                _db_chk.close()
            except:
                _n_loc_glob = 0
            
            if _n_loc_glob > 0 and df_loc_glob.empty:
                st.warning("⚠️ Locadores detectados. Calcule Honorarios en la pestaña '🧾 2' para un reporte completo.")
            
            try:
                buf_teso_f = generar_pdf_tesoreria(
                    df_planilla=df_plan_glob if not df_plan_glob.empty else None,
                    df_loc=df_loc_glob if not df_loc_glob.empty else None,
                    empresa_nombre=empresa_nombre,
                    periodo_key=periodo_key,
                    auditoria_data=aud_glob,
                    empresa_ruc=st.session_state.get('empresa_activa_ruc', ''),
                )
                st.download_button(
                    "🏦 Descargar Reporte de Tesorería (PDF)",
                    data=buf_teso_f,
                    file_name=f"TESORERIA_{periodo_key}.pdf",
                    mime="application/pdf",
                    use_container_width=True,
                    type="primary",
                    key="btn_teso_global_v_final"
                )
            except Exception: pass
>>>>
Validación final: Asegúrate de que no existan duplicados de la lógica de cierre en el mismo archivo y que el mensaje informativo use el componente st.info para destacar visualmente.