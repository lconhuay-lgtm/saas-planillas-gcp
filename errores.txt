Objetivo: Corregir el error UnboundLocalError en la vista de cálculo mensual asegurando la inicialización de variables antes del procesamiento de trabajadores.

Archivo a modificar:
presentation/views/calculo_mensual.py

Instrucciones de búsqueda y reemplazo:

Busca el bloque dentro de la función _render_planilla_tab donde se ejecuta el botón del motor de planilla y asegúrate de que la inicialización esté fuera de cualquier ambigüedad lógica:

Python
<<<<
    if st.button(f"🚀 Ejecutar Motor de Planilla - {periodo_key}", type="primary", use_container_width=True):
        st.session_state['ultima_planilla_calculada'] = True
        resultados = []
        auditoria_data = {}

        mes_calc  = int(mes_seleccionado[:2])
====
    if st.button(f"🚀 Ejecutar Motor de Planilla - {periodo_key}", type="primary", use_container_width=True):
        st.session_state['ultima_planilla_calculada'] = True
        
        # INICIALIZACIÓN GARANTIZADA
        resultados = []
        auditoria_data = {}

        mes_calc  = int(mes_seleccionado[:2])
>>>>
Análisis adicional:
Revisa que después del cambio realizado anteriormente para ocultar el botón en meses cerrados, no haya quedado código de cálculo (como el bucle for index, row in df_planilla.iterrows():) fuera del bloque if st.button(...). Todo el proceso de llenado de resultados debe ocurrir estrictamente dentro de la acción del botón o después de una inicialización clara.