Actúa como un Desarrollador Senior. Hemos detectado un bug lógico en el prorrateo de conceptos dinámicos para los trabajadores que ingresan a mitad de mes (mid-month hires).

Actualmente, el `factor_asistencia` divide `dias_laborados / dias_computables`. Esto causa que si un trabajador ingresa el 15 del mes y trabaja esos 15 días, el factor sea 15/15 = 1.0, pagándole el 100% de la movilidad, lo cual es incorrecto según el estándar laboral de Base 30.

DEBES MODIFICAR EL ARCHIVO `presentation/views/calculo_mensual.py`:

Busca dentro del bucle principal `for index, row in df_planilla.iterrows():` (aproximadamente en la sección de "TIEMPOS Y BASES FIJAS").

1. ELIMINA esta línea que calcula el factor de asistencia prematuramente:
`factor_asistencia = dias_laborados / dias_computables if dias_computables > 0 else 0.0`

2. Reubica la declaración de la variable `factor_asistencia` para que herede la misma lógica del "Mes Comercial Mixto" que utiliza el Sueldo Computable.

Modifica el bloque if/else existente para que quede exactamente así:

```python
            if total_ausencias == 0 and (not ingreso_este_mes or dias_computables >= dias_del_mes):
                sueldo_computable = sueldo_base_nominal
                factor_asistencia = 1.0
            else:
                sueldo_computable = max(0.0, valor_dia * dias_laborados)
                factor_asistencia = dias_laborados / 30.0