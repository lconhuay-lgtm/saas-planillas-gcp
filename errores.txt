Actúa como un Desarrollador Python Senior. El intento anterior falló porque las referencias de contexto no coincidieron exactamente con el código fuente. 

Realiza los siguientes cambios utilizando estrictamente la herramienta de reemplazo. RESPETA LOS ESPACIOS DE INDENTACIÓN EXACTOS MOSTRADOS.

1. Abre `presentation/views/calculo_mensual.py` y aplica este reemplazo:

*SEARCH*
            if aj_quinta != 0:
                desglose_descuentos['Ajuste 5ta Cat (Audit)'] = round(aj_quinta, 2)
                descuentos_manuales += aj_quinta
                obs_trab.append(f"Ajuste 5ta: S/ {aj_quinta:,.2f}")
*REPLACE*
            if aj_quinta != 0:
                retencion_quinta = max(0.0, retencion_quinta + aj_quinta)
                if retencion_quinta > 0:
                    desglose_descuentos['Retención 5ta Cat.'] = float(retencion_quinta)
                elif 'Retención 5ta Cat.' in desglose_descuentos:
                    del desglose_descuentos['Retención 5ta Cat.']
                obs_trab.append(f"Ajuste 5ta: S/ {aj_quinta:,.2f}")


2. Abre `presentation/views/ingreso_asistencias.py` y aplica este reemplazo (Definición de Tabs):

*SEARCH*
        # Mostrar mensajes de éxito diferidos (tras rerun)
        if st.session_state.get('_msg_asistencia'):
            st.toast(st.session_state.pop('_msg_asistencia'), icon="✅")

        # ── Cuatro pestañas principales (Estándar Corporativo) ──────────────────
        tab_plan, tab_loc, tab_notas, tab_ajustes = st.tabs([
            "📋 1. Planilla (5ta Cat.)",
            "🧾 2. Locadores (4ta Cat.)",
            "📝 3. Notas de Gestión",
            "🔧 4. Ajustes de Auditoría"
        ])
*REPLACE*
        # Mostrar mensajes de éxito diferidos (tras rerun)
        if st.session_state.get('_msg_asistencia'):
            st.toast(st.session_state.pop('_msg_asistencia'), icon="✅")

        # ── Cuatro pestañas principales (Estándar Corporativo) ──────────────────
        usuario_rol = st.session_state.get('usuario_rol', '')
        es_supervisor = usuario_rol in ['supervisor', 'admin']

        tabs_list = [
            "📋 1. Planilla (5ta Cat.)",
            "🧾 2. Locadores (4ta Cat.)",
            "📝 3. Notas de Gestión"
        ]
        if es_supervisor:
            tabs_list.append("🔧 4. Ajustes de Auditoría")

        tabs = st.tabs(tabs_list)
        tab_plan = tabs[0]
        tab_loc = tabs[1]
        tab_notas = tabs[2]


3. En el mismo archivo `presentation/views/ingreso_asistencias.py`, DEBES ENVOLVER todo el contenido de la pestaña 4 dentro del condicional de supervisor. Reemplaza el encabezado de la pestaña así:

*SEARCH*
        # ══════════════════════════════════════════════════════════════════════
        # TAB 4 — AJUSTES DE AUDITORÍA (SAP/ORACLE STYLE)
        # ══════════════════════════════════════════════════════════════════════
        with tab_ajustes:
            st.subheader("🔧 Ajustes y Regularizaciones de Nómina")
*REPLACE*
        # ══════════════════════════════════════════════════════════════════════
        # TAB 4 — AJUSTES DE AUDITORÍA (SAP/ORACLE STYLE)
        # ══════════════════════════════════════════════════════════════════════
        if es_supervisor:
            tab_ajustes = tabs[3]
            with tab_ajustes:
                st.subheader("🔧 Ajustes y Regularizaciones de Nómina")

IMPORTANTE PARA EL PASO 3: Como hemos añadido el bloque `if es_supervisor:`, todo el código que estaba debajo de `with tab_ajustes:` hasta el final de la función `render()` debe recibir 4 espacios extra de indentación para no romper la sintaxis de Python. Por favor, asegúrate de indentar correctamente todo ese bloque.