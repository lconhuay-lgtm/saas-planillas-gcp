Actúa como un Desarrollador Senior de Python experto en Streamlit.
Necesito optimizar la experiencia de usuario en la descarga de la ficha de personal. Actualmente requiere dos clics (un botón activa un estado, recarga la página, y muestra un segundo botón de descarga). 
Debemos cambiarlo para que el ícono "📄" sea directamente un `st.download_button`, generándose el buffer de memoria en el mismo bucle de la lista de trabajadores.

Aplica EXCLUSIVAMENTE los siguientes dos bloques de SEARCH/REPLACE en el archivo `presentation/views/maestro_trabajadores.py`. No modifiques ninguna otra parte de la interfaz.

### BLOQUE 1: Eliminar la lógica de estado intermedio al inicio de la lista
Busca la sección `# --- Lógica de Ficha PDF ---` y elimínala, dejando solo el texto del caption inferior.

<<<<
            # --- Lógica de Ficha PDF ---
            ficha_id = st.session_state.get('_generar_ficha_id')
            if ficha_id:
                t_ficha = db.query(Trabajador).filter_by(id=ficha_id).first()
                if t_ficha:
                    st.info(f"📄 Generando ficha de **{t_ficha.nombres}**...")
                    buf_ficha = generar_pdf_ficha_trabajador(
                        t_ficha, empresa_nombre, 
                        st.session_state.get('empresa_activa_ruc', ''), 
                        regimen_empresa
                    )
                    col_fd1, col_fd2 = st.columns([1, 4])
                    col_fd1.download_button(
                        "📥 Descargar PDF", data=buf_ficha, 
                        file_name=f"Ficha_{t_ficha.num_doc}.pdf", mime="application/pdf", type="primary"
                    )
                    if col_fd2.button("✖️ Cerrar"):
                        st.session_state.pop('_generar_ficha_id', None)
                        st.rerun()
                    st.markdown("---")
            # ---------------------------

            st.caption(f"{len(trabajadores_db)} trabajador(es) registrado(s)")
====
            st.caption(f"{len(trabajadores_db)} trabajador(es) registrado(s)")
>>>>


### BLOQUE 2: Reemplazar el botón normal por el botón de descarga directa en el bucle
Busca la línea donde se dibuja el botón `c_pdf.button` dentro del bucle de trabajadores y reemplázalo para generar y asignar el PDF directamente a un `download_button`.

<<<<
                    if c_pdf.button("📄", key=f"pdf_{t.id}", help="Generar Ficha PDF"):
                        st.session_state['_generar_ficha_id'] = t.id
                        st.rerun()

                    if c5.button("✏️", key=f"edit_{t.id}", help="Editar trabajador"):
====
                    buf_ficha = generar_pdf_ficha_trabajador(
                        t, empresa_nombre, 
                        st.session_state.get('empresa_activa_ruc', ''), 
                        regimen_empresa
                    )
                    c_pdf.download_button(
                        "📄", 
                        data=buf_ficha, 
                        file_name=f"Ficha_{t.num_doc}.pdf", 
                        mime="application/pdf", 
                        key=f"pdf_dl_{t.id}", 
                        help="Descargar Ficha PDF"
                    )

                    if c5.button("✏️", key=f"edit_{t.id}", help="Editar trabajador"):
>>>>