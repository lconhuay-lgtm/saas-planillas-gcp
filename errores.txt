Actúa como un Ingeniero de Software. Estamos recibiendo un error TypeError: 'tipo_documento' is an invalid keyword argument for Trabajador al intentar guardar un nuevo trabajador. Esto sucede porque agregamos la columna tipo_documento a la base de datos mediante migraciones directas en SQL, pero no actualizamos el modelo ORM correspondiente para reflejar este nuevo campo.

INSTRUCCIÓN:
Abre el archivo infrastructure/database/models.py y localiza la clase Trabajador. Agrega la columna tipo_documento debajo del bloque de "Datos Personales", justo después de tipo_doc o correo_electronico.

Python
    # Añade esta línea:
    tipo_documento = Column(String(2), default='01', server_default='01')
El bloque de "Datos Personales" en la clase Trabajador debería quedar similar a esto:

Python
    # Datos Personales
    tipo_doc = Column(String(20))    # 01=DNI, 04=CE, 07=Pasaporte
    tipo_documento = Column(String(2), default='01', server_default='01') # NUEVA COLUMNA PARA PLAME
    num_doc = Column(String(20), index=True, nullable=False)
Por favor, realiza esta única modificación en infrastructure/database/models.py.