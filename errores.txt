Actúa como un Arquitecto de Software Senior. Tenemos que corregir un bug visual/lógico en la asignación de "Ajustes de Auditoría" para la 5ta Categoría y añadir una restricción de seguridad basada en roles (RBAC) a la interfaz de pestañas.

DEBES MODIFICAR ESTRICTAMENTE LOS SIGUIENTES 2 ARCHIVOS:

1. Modificar `presentation/views/calculo_mensual.py`:
- Busca la sección `--- APLICACIÓN DE AJUSTES DE AUDITORÍA (MANUALES) ---`.
- Dentro de esa sección, busca el bloque `if aj_quinta != 0:`.
- REEMPLAZA todo el bloque `if aj_quinta != 0:` por el siguiente código (asegurándote de no sumarlo a descuentos_manuales):
```python
            if aj_quinta != 0:
                # Modificamos directamente la variable de retención para que impacte la columna "Ret. 5ta Cat."
                retencion_quinta = max(0.0, retencion_quinta + aj_quinta)
                if retencion_quinta > 0:
                    desglose_descuentos['Retención 5ta Cat.'] = float(retencion_quinta)
                elif 'Retención 5ta Cat.' in desglose_descuentos:
                    del desglose_descuentos['Retención 5ta Cat.']
                obs_trab.append(f"Ajuste 5ta: S/ {aj_quinta:,.2f}")
Modificar presentation/views/ingreso_asistencias.py:

Busca la definición actual de las pestañas (alrededor de la línea 100):
tab_plan, tab_loc, tab_notas, tab_ajustes = st.tabs([...])

REEMPLAZA esa declaración por esta lógica dinámica de permisos:

Python
        # ── Cuatro pestañas principales (Estándar Corporativo) ──────────────────
        usuario_rol = st.session_state.get('usuario_rol', '')
        es_supervisor = usuario_rol in ['supervisor', 'admin']

        tabs_list = [
            "📋 1. Planilla (5ta Cat.)",
            "🧾 2. Locadores (4ta Cat.)",
            "📝 3. Notas de Gestión"
        ]
        if es_supervisor:
            tabs_list.append("🔧 4. Ajustes de Auditoría")

        tabs = st.tabs(tabs_list)
        tab_plan = tabs[0]
        tab_loc = tabs[1]
        tab_notas = tabs[2]
Luego, dirígete hacia el final del archivo y busca la línea:
with tab_ajustes:

REEMPLÁZALA por:

Python
        if es_supervisor:
            tab_ajustes = tabs[3]
            with tab_ajustes:
IMPORTANTE: Selecciona todo el código que originalmente estaba dentro del antiguo with tab_ajustes: (desde st.subheader("🔧 Ajustes... hasta el final de la función) y agrégale un nivel de tabulación adicional (4 espacios) hacia la derecha para que quede correctamente anidado dentro del nuevo if es_supervisor:.

Realiza los cambios respetando la indentación de Streamlit.